on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - master

name: Detalle Análisis SonarQube

jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      - name: Get SonarQube Quality Gate Status
        id: sonarqube_status
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          curl -X GET -u "${SONAR_TOKEN}": -o sonarqube.json '${{ secrets.SONAR_HOST_URL }}/api/qualitygates/project_status?projectKey=Spuchifai'
          echo "::set-output name=sonarqube_status::$(cat sonarqube.json)"

      - name: Get SonarQube Measures
        id: sonarqube_measures
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          curl -X GET -u "${SONAR_TOKEN}": -o sonarqube_measures.json '${{ secrets.SONAR_HOST_URL }}/api/measures/component_tree?additionalFields=metrics&ps=500&asc=false&metricSort=new_duplicated_lines_density&s=metricPeriod&metricSortFilter=withMeasuresOnly&metricPeriodSort=1&component=Spuchifai&metricKeys=new_duplicated_lines_density,new_line_coverage,code_smells,new_bugs,new_vulnerabilities,new_security_hotspots_reviewed&strategy=leaves'
          echo "::set-output name=sonarqube_measures::$(cat sonarqube_measures.json)"

      - name: Comment PR
        uses: thollander/actions-comment-pull-request@v2
        with:
          message: |
            Detalle del Análisis de SonarQube:

            Estado del Análisis de SonarQube: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.status == 'OK' && '✅ Exitoso' || '❌ Fallido' }}

            Condiciones:
              - Métrica: Nuevo Índice de Fiabilidad
                Estado: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[0].status == 'OK' && '✅ Exitoso' || '❌ Fallido' }}
                Umbral de Error: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[0].errorThreshold }} (Cuanto menor es mejor)
                Valor Actual: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[0].actualValue }}
              - Métrica: Nuevo Índice de Seguridad
                Estado: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[1].status == 'OK' && '✅ Exitoso' || '❌ Fallido' }}
                Umbral de Error: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[1].errorThreshold }} (Cuanto menor es mejor)
                Valor Actual: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[1].actualValue }}
              - Métrica: Nuevo Índice de Mantenibilidad
                Estado: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[2].status == 'OK' && '✅ Exitoso' || '❌ Fallido' }}
                Umbral de Error: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[2].errorThreshold }} (Cuanto menor es mejor)
                Valor Actual: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[2].actualValue }}
              - Métrica: Nueva Densidad de Líneas Duplicadas
                Estado: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[3].status == 'OK' && '✅ Exitoso' || '❌ Fallido' }}
                Umbral de Error: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[3].errorThreshold }} (Cuanto menor es mejor)
                Valor Actual: ${{ fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.conditions[3].actualValue }}

            Detalles de las Métricas:

            Componente: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].name }} (${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].path }}):
              - Métrica: Índice de Líneas Duplicadas en Código Nuevo
                Estado: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[0].period.value }}%$'\n'${{ if fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[0].bestValue }}(mejor valor){{ else }}(peor valor){{ endif }} ⚙️$'\n'📝 Cantidad de Líneas Duplicadas: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[0].value }}

              - Métrica: Índice de Cobertura de Líneas en Código Nuevo
                Estado: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[1].period.value }}%$'\n'${{ if fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[1].bestValue }}(mejor valor){{ else }}(peor valor){{ endif }} 🔍$'\n'📝 Cantidad de Líneas Cubiertas: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[1].value }}

              - Métrica: Code Smells (Malos Olores)
                📦 Cantidad de Code Smells: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[2].value }}

              - Métrica: Nuevos Bugs
                🐞 Cantidad de Bugs: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[3].value }}

              - Métrica: Nuevas Vulnerabilidades
                🔒 Cantidad de Vulnerabilidades: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[0].measures[4].value }}

            Componente: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].name }} (${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].path }}):
              - Métrica: Índice de Líneas Duplicadas en Código Nuevo
                Estado: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].measures[0].period.value }}%$'\n'${{ if fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].measures[0].bestValue }}(mejor valor){{ else }}(peor valor){{ endif }} ⚙️$'\n'📝 Cantidad de Líneas Duplicadas: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].measures[0].value }}

              - Métrica: Code Smells (Malos Olores)
                📦 Cantidad de Code Smells: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].measures[1].value }}

              - Métrica: Nuevos Bugs
                🐞 Cantidad de Bugs: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].measures[2].value }}

              - Métrica: Nuevas Vulnerabilidades
                🔒 Cantidad de Vulnerabilidades: ${{ fromJson(steps.sonarqube_measures.outputs.sonarqube_measures).components[1].measures[3].value }}

      - name: Check SonarQube Analysis Status
        if: steps.sonarqube_status.outputs.sonarqube_status != 'undefined' && fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.status == 'ERROR'
        run: exit 1

      - name: Block PR on SonarQube Analysis Failure
        if: steps.sonarqube_status.outputs.sonarqube_status != 'undefined' && fromJson(steps.sonarqube_status.outputs.sonarqube_status).projectStatus.status == 'ERROR'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "The SonarQube analysis failed. Blocking the PR..."
          pr_number="${{ github.event.pull_request.number }}"
          owner="${{ github.repository_owner }}"
          repo="${{ github.repository }}"
          response=$(curl -X POST -v \
            -H "Authorization: Bearer $GITHUB_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/$owner/$repo/issues/$pr_number/lock" 2>&1)
          echo "$response"
